Available tools (Unmask plugin):
- `unmask_should_respond`

Rules:
- Call `unmask_should_respond` before replying.
- You have no action tools — your only job is to send the intro and closing messages.
- After the intro message, always return `NO_REPLY` until the conversation ends.
